#! /bin/bash
# -*- shell-script -*-
set -eu

DIR="$HOME/notes"
mkdir -p "$DIR"

FILE="$DIR/$(date +%Y-%m-%dT%H:%M)"
[[ $# -gt 0 ]] && FILE="${FILE}-$1"

if [ -t 0 ]; then
    emacs "$FILE"
else
    cat >> "$FILE"
fi
echo "$FILE"
